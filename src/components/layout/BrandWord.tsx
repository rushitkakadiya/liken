export function BrandWord({ className }: { className?: string }) {
  return (
    <span className={className}>
      {"like"}<span className="text-[#ee296b] font-semibold">n</span>
    </span>
  );
}
